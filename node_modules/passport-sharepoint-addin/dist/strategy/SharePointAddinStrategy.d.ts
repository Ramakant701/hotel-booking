/// <reference types="express" />
/// <reference types="passport" />
import * as passport from 'passport-strategy';
import { AuthenticateOptions } from 'passport';
import { Request } from 'express';
import { IOAuthConfig } from '../common/IOAuthConfig';
export declare class SharePointAddinStrategy extends passport.Strategy {
    name: string;
    private oauth;
    private callbackUrl;
    private verify;
    constructor(options: IOAuthConfig, callbackUrl: string, verify: any);
    authenticate(req: Request, options: AuthenticateOptions): void;
    private onReturnCallback(req, options);
    private getAccessToken(authData, req);
    private verifyAppToken(req, spAppToken);
    private ensureTrailingSlash(url);
}
