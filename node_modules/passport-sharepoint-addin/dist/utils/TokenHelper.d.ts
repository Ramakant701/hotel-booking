import * as Promise from 'bluebird';
import { IOAuthConfig } from '../common/IOAuthConfig';
import { IAccessToken } from '../common/IAccessToken';
import { IAuthData } from '../strategy/IAuthData';
export declare class TokenHelper {
    static getUserAccessToken(authData: IAuthData, oauth: IOAuthConfig, url: string): Promise<IAccessToken>;
    static getAppOnlyAccessToken(authData: IAuthData, oauth: IOAuthConfig, url: string): Promise<IAccessToken>;
}
